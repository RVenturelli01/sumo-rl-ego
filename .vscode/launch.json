{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Run current file (repo root)",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}