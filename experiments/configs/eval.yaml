wandb:
  project: "sre_eval"
  run_name: ${mode}
  notes: 
  sync_tensorboard: false



n_episodes: 100
seed: 0

mode: model   # model | policy_id | policy_class

model_path: null  # (if mode=model)

policy_id: null  # (if mode=policy_id)

policy_class:  
  _target_: sre.policies.MyPolicy    # (if mode=policy_class)


env_id: "highway_e1_o2_rfast"  # (if  mode=policy_id or  mode=policy_class)


hydra:
  run:
    dir: .
  output_subdir: null