# @package _global_

/env: highway_fast

/rl: dqn_finetune

name: finetune_${env.name}_${rl.algorithm}

model_path: ???

seed: 0

hydra:
  job:
    chdir: true
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}_${name}
  sweep:
    dir: outputs/multirun/${now:%Y-%m-%d_%H-%M-%S}_${name}
    subdir: ${hydra.job.num}
