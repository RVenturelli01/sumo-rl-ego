run:
  name: "play"
  seed: 0
  manual: false
  display: true
  output_dir: ${hydra:runtime.output_dir}
  

# only one between: model_id | model_path | policy_id | policy_class 
source:  
  model_id: sac-fast 
  model_path: null
  policy_id: null
  policy_class: 
    __target__: null


env:
  id: "HighwayEgo-v0"
  kwargs:
    reward: "fast"
    ego: "continuous"


hydra:
  run:
    dir: .
  output_subdir: null
