# Top-level runtime dependencies for sumo-rl-ego.
# This file intentionally lists only direct project dependencies, not a full
# frozen environment.
#
# CPU-only setup note:
# Install a CPU build of PyTorch before this file if you want to avoid CUDA
# wheels entirely, for example from https://pytorch.org/get-started/locally/.

gymnasium==1.2.3
numpy==2.2.6

hydra-core>=1.3.0
omegaconf>=2.3.0
tqdm==4.67.3

stable_baselines3==2.7.1
stable-baselines3[extra]==2.7.1
tensorboard==2.20.0

# SUMO Python bindings. The code uses traci for GUI mode and libsumo for
# headless mode.
libsumo==1.26.0
traci==1.26.0
sumolib==1.26.0

# Optional experiment tracking used by experiments/train.py and eval.py.
wandb>=0.18.0
